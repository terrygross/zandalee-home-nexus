import * as React from "react";

/**
 * Minimal toaster stub — safe no-op.
 * Replace later with shadcn/sonner if you want real toasts.
 */
export function Toaster(): JSX.Element {
  return <></>;
}
export default Toaster;